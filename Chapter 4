usernames = ['Grant', 'Aleks', 'sarah', 'Alexnder', 'emma']

for username in usernames:
    if username == 'Grant':
        print(f"Hello Grant, would you like to see a status report?")
    else:
        print(f"Hello {username}, thank you for logging in again.")
